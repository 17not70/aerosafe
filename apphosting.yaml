
# Firebase App Hosting configuration for AeroSafe Insights
# https://firebase.google.com/docs/app-hosting/configure

runtime: nodejs20
entrypoint: npm run start

runConfig:
  maxInstances: 1

env:
  - variable: NODE_ENV
    value: "production"
  - variable: NEXT_TELEMETRY_DISABLED
    value: "1"
